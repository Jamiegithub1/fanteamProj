# M9 - Web-App Frontend

## Status

Completed on 2026-04-24.

## Implemented

- Replaced the setup placeholder with a usable projection dashboard.
- Added projection table sorted by fantasy points.
- Added player search.
- Added date and team filters.
- Added source health strip with latency/status.
- Added manual full refresh button.
- Added backend `GET /projections` endpoint for the frontend.

## Notes

The live database currently has no projection rows because no stable live player-prop payload has been collected yet. The UI handles that empty state cleanly and is ready for populated data.

## Verification

- `pytest -q`
- `npm run build`
- `GET /projections`
- Local frontend served by Vite at `http://localhost:5173`
