# M1 - Projekt Setup

Datum: 2026-04-24

## Ergebnis

- FastAPI Backend initialisiert
- React + TypeScript Frontend initialisiert
- PostgreSQL via Docker Compose integriert
- `.env.example` angelegt
- Basis-README angelegt
- Backend und Frontend lokal erreichbar

## Verifikation

- `pytest`: 1 Test bestanden
- `npm run build`: erfolgreich
- `docker compose config`: erfolgreich
- `docker compose up --build -d`: Stack gestartet
- `GET /health`: `{"status":"ok","service":"backend"}`
- `GET /health/db`: `{"status":"ok"}`
- Frontend `http://localhost:5173`: HTTP 200
